"""CI 게이트 실발동 확인용 일회성 프로브 — 다음 커밋에서 제거한다.

원칙 7: 새로 건 게이트는 **일부러 위반시켜** 막히는지 본다. 통과 한 번은 결론이 아니다.
아래 import는 쓰이지 않는다 → ruff `F401`이 걸려야 하고, `lint` 잡이 빨간불이어야 한다.
`scripts/**`의 per-file-ignores는 `C901`·`T201`뿐이라 `F401`은 살아 있다.
mypy 대상은 `dagster/dockerfile.d/src/src`라 이 파일은 `defs` 잡을 건드리지 않는다
(=같은 실행 안에 대조군이 남는다 — 게이트가 통째로 깨진 게 아니라 겨냥한 축만 걸렸음을 보인다).
"""

import os

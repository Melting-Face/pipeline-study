# 워커 편성 (도메인 × 축)

> 에이전트 규약 인덱스는 [`../agents.md`](../agents.md).
> 이 문서는 **누가 무엇을 맡는가**를 담는다.
> 권한·프론트매터는 [`permissions.md`](permissions.md), 승인 절차는 [`gates.md`](gates.md).

축(구현 / 실측 대조 / 체계 감사)은 두 도메인이 **동일**하다. 판단 규칙을 하나로 유지하기 위함이다.

| 축 | 데이터 | 인프라 | 분석 | 공개 | 무엇을 묻는가 |
| --- | --- | --- | --- | --- | --- |
| **구현·수정** | `data-engineer` | `devops-engineer` | `analyst` | `tech-writer` | 만들었는가 |
| **실측 대조** | `data-verifier` | `devops-verifier` | ← 재사용 | ← 재사용 | 실제가 선언과 같은가 |
| **체계 감사** | `data-qa` | `devops-qa` | ← 재사용 | ← 미분화 | 재발을 막을 상시 장치가 있는가 |
| **외부 근거**(공통) | `researcher` | `researcher` | `researcher` | `researcher` | 주장이 **1차 출처**와 같은가 |
| **노출·규제**(공통) | `security` | `security` | `security` | `security` | 새어나가는가 · 규제를 지키는가 |
| **관측·기록**(계층 밖) | `archivist` | `archivist` | `archivist` | `archivist` | 기록이 사실과 맞는가 |
| **스킬 배선**(계층 밖) | `skill-matcher` | `skill-matcher` | `skill-matcher` | `skill-matcher` | 맞는 스킬을 물고 있는가 |
| **데이터 반출**(축 아님·예외) | `data-extractor` | — | `data-extractor` | — | 명세대로 뽑았는가 · **새지 않는가** |

## 축을 늘리지 않는 이유

🔴 **워커 신설의 근거는 "역할이 논리적으로 존재한다"가 아니라 "배정이 반복돼 병목이 됐다"**(Rule of Three)다.

**"테스트"는 축이 아니다 — 3축에 분해된다.** `tester` 같은 워커를 두지 않는다(검토·반려됨).
테스트라는 말이 한 단어라서 한 명이 필요해 보이지만 실제 행위는 이미 세 축에 주인이 있다 —
**쓰는 것**=`data-engineer` · **결과를 원천과 대조하는 것**=`data-verifier` ·
**커버리지·게이트를 감사하는 것**=`data-qa`. 넷째를 두면 "스키마 테스트 추가"를 누구에게 줄지
**매 배정마다 판정**해야 하고, 이는 3종 세트의 경계가 없애려던 바로 그 비용이다.

**매체도 축이 아니다** — "티스토리용"·"발표자료용"으로 워커를 나누지 않는다.
산출물·도구·리스크가 전부 같아서 나누면 글 하나마다 배정을 판정하게 된다.
매체 차이는 `tech-writer` 지시문의 **포맷 프로파일**로 흡수한다.

**분석·공개는 새 축이 아니라 새 도메인**이라 3종 세트를 복제하지 않았다.
구현 축 1명(`analyst`·`tech-writer`)만 두고 판정은 데이터 도메인 워커를 **그대로 재사용**한다.
판정 부하가 실제로 생기면 그때 분화한다.

### `data-extractor`는 명시적 예외다

표의 마지막 행은 **축이 아니라** 예외를 눈에 띄게 두려고 붙인 것이다.
실제로는 데이터 도메인 **구현 축의 두 번째 워커**이고 *"구현 축 1명씩"* 원칙과 어긋난다.

**신설 근거는 업무가 아니라 노출 통제다.** `analyst`와 방법은 겹치지만 산출물이
**결론 ↔ 데이터 그 자체**로, 착지가 **저장소 안 ↔ 밖**으로 갈린다.
분리하면 추출 워커는 저장소에 **아무것도 못 쓴다**(기계 강제).

🔴 **이 신설을 선례로 삼아 다른 워커를 늘리지 않는다.** 예외의 근거는 "역할이 다르다"가 아니라
**"한 워커가 원천 반출과 커밋 경로 쓰기를 동시에 가지면 안 된다"** 이고,
그 조건이 성립하지 않으면 예외도 없다.

## 경계

- **`analyst` ↔ `data-engineer`**: gold 모델(dbt SQL)의 **정의는 `data-engineer`가 소유**한다.
  `analyst`는 SQL 초안·grain·근거를 **제안만** 하고 직접 고치지 않는다 — 파이프라인 정의의
  단일 소유자를 흐리지 않기 위함이다. `analyst`의 쓰기는 `notebooks/**`·`docs/analyses/**` 뿐이다.
- **`security`(노출·규제) ↔ `devops-qa`(운영 신뢰성·재현성)** 는 관점이 다르다 — 중첩 금지.
  겹치는 항목은 `devops-qa`가 중복 제기하지 않고 **`security` 확인 요청으로 넘긴다**.
- **`skill-matcher`(계층 밖)는 `archivist`와 대칭**이다 — `archivist`가 "저널 정합",
  `skill-matcher`가 "스킬 배선 정합"을 본다. `*-qa`와의 경계: `*-qa`는 **도메인의 검증 체계**,
  `skill-matcher`는 **워커에 물린 스킬**을 본다. 스킬 **설치·lock 편집은 하지 않는다**(공급망·비가역).
- **`researcher`는 도메인 공통 축**이다. 경계는 **안/밖** — 저장소 **안** 탐색은 각 도메인 워커,
  저장소 **밖** 1차 출처는 `researcher`.

### `tech-writer` — 저장소의 문서 소유자

쓰기 범위가 `docs/posts/**` 한정에서 **`docs/**` 전체 + 최상위 `README.md`** 로 넓어졌다
문서 일관성의 단일 소유자를 두기 위함이다.

**넓힌 대가는 「기계가 못 가르는 경계」 둘이다.** 가드는 디렉터리 단위라 아래를 구분하지 못하고
**규율로만** 지켜진다.

- **`docs/analyses/**` 는 `analyst`와 이중 소유** — 내부 결론의 **저자는 `analyst`**,
  `tech-writer`는 **표현만** 손본다(수치·코호트·결론 변경 금지).
- **`docs/conventions/**` 는 규약 정본** — `tech-writer`는 supervisor 결정을 **받아적을 뿐**
  규칙을 신설·변경하지 않는다. 문안 정합(링크·목차·요약 동기화)은 `tech-writer` 몫이다.
  2026-08-22부터 이 경계에는 `ask` 프롬프트조차 없다 — 즉 **아무도 안 물어보는 구간**이고,
  지켜지는지는 워커 반환으로만 관측된다.
- `CLAUDE.md`는 `docs/` 밖이라 **여전히 supervisor 소관**이다(가드가 실제로 막는 층).

✅ **셋째 경계는 규율에서 기계 강제로 올라갔다** — **`docs/security.md`·`docs/skills.md`**
는 **그 워커를 판정하는 근거 문서**라, 판정 대상이 판정 기준을 고치면 통제가 성립하지 않는다.
`worker_path_guard.py`의 **`except` 축**으로 `deny`한다. **문안 정합조차 예외가 아니다** —
고칠 것이 보이면 지적만 반환한다.

**독자는 둘이다** — `docs/posts/**`는 **모르는 사람**, 나머지 문서는 **아는 사람**이 읽는다.
공개물의 주장은 **저장소에 이미 있는 결론**이거나 `researcher`가 1차 출처로 지지한 것이어야 하고,
`tech-writer`는 **새 결론을 만들지 않는다**. 정본 [`../publishing.md`](../publishing.md).

**발행(업로드)은 어느 워커도 하지 않는다.** 외부 발신은 **비가역**이라 마지막 게이트는
**사람**이 갖는다(자동화하지 않는 것이 설계다).

## 외부 접촉은 네 축으로 가른다 — 나가는 것 3 + 들어오는 것 1

종전 교리 문장은 *"저장소의 유일한 외부 네트워크 접촉 지점은 `researcher`"* 였다. **세 가지로 부정확하다.**

1. **사실 서술로서 틀렸다** — `podman push`·`npx`·`uv`·`helm`이 전부 네트워크다.
   **아무도 그것들을 세고 있지 않았다.** 새 워커가 생겨서 거짓이 된 것이 아니라
   **처음부터 거짓이었고, 세는 대상이 좁아 검산을 통과했다.**
2. **위험 분류로서 두 축을 뭉쳤다** — **질의 유출(DUA)** 과 **외부 코드 반입(공급망)** 은
   위험 등급도 통제 지점도 다르다. 뭉쳐 두면 한쪽 통제를 다른 쪽의 근거로 읽는다.
3. **규칙의 *이유*까지 틀리게 전달했다.** *"유일한 접촉 지점"* 은 **접촉 횟수를 세는 말**처럼 읽히지만
   실제로 지키려던 것은 **① 질의가 무엇을 담는가**와 **② 가져온 것이 어디에 앉는가** 둘이다.
   **횟수는 대리 지표였을 뿐이고, 대리 지표가 정본 자리에 앉으면 원래 목적이 보이지 않게 된다.**

**나가는 것 — 3축**

| 축 | 통제 지점 | 성격 |
| --- | --- | --- |
| **질의 유출(DUA)** | `researcher` | 질의 자체가 외부 발신 · **워커 자기 규율이 실효의 전부** |
| **외부 코드 반입** | **전담 워커 없음** — 계획만 → `security` 컨펌 → 🚦 사람이 집행 | 공급망 · **비가역** |
| **빌드·설치 계열**(`podman push`·`uv`·`helm`) | 통제 대상 아님(빌드 파이프라인) | **원래부터 그 문장 밖이었다** |

- **두 번째 축을 "워커가 있다"고 쓰지 않는다.** 구현 워커 신설이 검토됐으나 **`security` 반려 + 철회**됐다.
  현행 통제는 `skill-matcher`의 `researcher` 릴레이 경로다 — **그 칸은 비어 있지 않았다.**
- **세 번째 축을 "통제 대상 아님"으로 적는 것은 면제가 아니라 *경계의 명시*다.**
  이 축이 위험해지면(신뢰할 수 없는 레지스트리·차트 소스) 두 번째 축으로 승격시켜 다룬다.

**들어오는 것 — 격리 축**

위 3축은 전부 "무엇이 나가는가"다. 네 번째 축은 **"무엇이 들어와서 어디에 앉는가"** 이고 앞의 셋과 **직교한다.**
통제 수단은 `researcher` **릴레이 구조 그 자체** — 오염이 그 워커의 **반환문에 갇힌다**.
🔴 **기계 강제는 하나도 없다.**

- **릴레이는 우회로가 아니라 격리 장치다.** 2왕복이 *"번거로운 절차"* 로 읽히면
  **다음 사람이 지름길을 만든다.** 왕복 수를 줄이는 최적화는 이 축을 **직접 깎는다** —
  절차의 비용이 곧 그 절차의 기능인 자리다.
- **supervisor가 직접 외부에 닿는 것은 "워커를 안 쓴 것"이 아니라 "격리를 뚫은 것"이다.**
  `researcher`가 가져오면 오염은 반환문에 갇히지만, supervisor가 직접 `WebFetch`하면
  **최상위 컨텍스트에 외부 콘텐츠가 직접 착지**한다. ⇒ 격리 측면에서는 릴레이보다 나쁘다.
- **두 축은 같은 행위에 대해 반대로 움직일 수 있다.** supervisor가 직접 외부 조회를 하면
  질의문에 내부 데이터가 없는 한 **유출축은 통과**하는데 **격리축은 악화**된다.
  ⇒ 두 축을 한 칸에 묶으면 이 **반대 운동을 구조적으로 못 본다.**
- 회피 불가능한 상황이면 `security`는 반려가 아니라 **「조건부 승인」** 을 낸다.
  **규율 위반이 아니라 「구조적 제약의 결과」로 분류한다** — 분류를 틀리면 대응도 틀린다.
  규율 위반이면 **워커를 고치고**, 구조적 제약이면 **제약을 푸는 쪽** 또는 **보상 통제**를 건다.

## 전문 워커 3종 세트의 경계 (중첩 금지)

`verifier`/`qa`는 통상 의미가 겹치므로 **축을 명시**해 나눈다.

| 축 | 보는 대상 | 질문 | 데이터 | 인프라 |
| --- | --- | --- | --- | --- |
| **구현** | 코드·선언 파일 | "어떻게 만드는가" | `data-engineer` | `devops-engineer` |
| **인스턴스** | 지금 존재하는 실체 | "실제가 맞는가" | `data-verifier` | `devops-verifier` |
| **체계** | 검증 장치·게이트 | "상시 장치가 있는가" | `data-qa` | `devops-qa` |

흐름(양 도메인 동일): `*-engineer` 구현 → `*-verifier` 실측 대조 → `*-qa`가 상시 게이트로 만들 계획 반환 →
승인 후 `*-engineer`가 작성 → **`*-qa`가 작성된 테스트를 사후 채점**.
**판정자는 절대 스스로 고치지 않는다.**

🔴 **마지막 사후 채점은 생략 가능한 단계가 아니다** — 판정자가 쓰지 않으므로 **구현자가 자기 코드의
테스트를 쓴다.** 자기가 통과시킬 수 있게 쓴 테스트는 "통과"가 *검사했다*가 아니라 *실행됐다*뿐일 수
있다([`../../philosophy.md`](../../philosophy.md) 원칙 7). 그래서 `*-qa`는 **계획을 낸 뒤 한 번 더 불려**
테스트가 grain·경계값·negative case를 실제로 짚는지 채점한다 — **작성자와 채점자를 분리하는 유일한 지점**이다.

**테스트 배정 판정표**

| 요청 | 배정 | 왜 |
| --- | --- | --- |
| "`sofa`에 스키마 테스트 추가" | `data-engineer` | 파일을 쓰는 일 = 구현 축 |
| "이 테스트가 실제로 위반을 잡나" | `data-qa` | 검증 장치의 유효성 = 체계 축 |
| "테스트는 통과하는데 값이 이상하다" | `data-verifier` | 값 자체 = 실측 축 |
| "게이트를 일부러 위반시켜 막히는지" | **supervisor 직접** | 계층 밖 · 전담 워커 미신설 |

**분석 흐름**: `analyst` 탐색·리포트 → gold 승격 **제안** → 승인 후 `data-engineer`가 모델 구현 →
`data-qa`가 grain·범위 테스트 보강 계획 → `security`가 산출물 반출 점검.

## 네이티브 구현 (`.claude/agents/`)

계층은 **Claude Code 서브에이전트**로 구현되어 있다. 각 정의는 종료 시 저널용 결과를
**반환**하도록 지시문에 못박는다.

| 파일 | 역할 |
| --- | --- |
| (없음) | **supervisor** — 메인 루프(대화 주체). 파일로 만들 수 없다 |
| (내장 `general-purpose`) | 단일 작업 워커 — 별도 파일 불필요(YAGNI) |
| `security.md` | 비밀누출·거버넌스·인프라 노출·ISMS-P 준수를 **읽기 전용** 점검 |
| `data-engineer.md` | Dagster 에셋·dbt 모델·S3→Iceberg 적재를 **구현·수정** |
| `data-verifier.md` | 적재된 **실제 데이터 값**을 원천과 대조(**읽기 전용**) |
| `data-qa.md` | dbt 테스트 커버리지·게이트 등 **검증 체계**를 감사(**읽기 전용**) |
| `data-extractor.md` | 명세대로 데이터를 뽑아 **저장소 밖**에만 쓴다 |
| `devops-engineer.md` | compose·Dockerfile·k8s manifest·Terraform HCL을 **구현·수정** |
| `devops-verifier.md` | 실행 중 인프라의 **런타임 상태**를 선언과 대조(**읽기 전용**) |
| `devops-qa.md` | 인프라 **선언 파일·게이트 체계**를 감사(**읽기 전용**) |
| `analyst.md` | 레이크하우스로 **질문에 답한다**. gold 승격은 **제안만** |
| `tech-writer.md` | 저장소의 **문서 소유자**. **발행 금지** |
| `researcher.md` | 외부 **1차 출처**를 제목·URL·절과 함께 반환(**읽기 전용**) |
| `archivist.md` | 저널 정합성·누락 점검, MOC 유지(관측·기록만) |
| `skill-matcher.md` | **스킬↔워커 배선 감사**(**읽기 전용**), 재배선 계획만 반환 |

- **전문 워커 = 읽기 전용 원칙**: 판정이 목적인 워커에는 `Write`/`Edit`를 주지 않고,
  나아가 `disallowedTools`로 **명시 거부**한다(미부여는 상속 경로가 생기면 뚫린다).
  구현이 목적인 워커는 예외로 쓰기를 갖되 **비가역 작업은 계획만 반환**한다.
  `analyst`는 여기에 더해 **테이블을 만들거나 덮어쓰는 실행**과 **정의 파일 수정**도 하지 않는다.
- **새 `.claude/agents/*.md`는 추가 직후 같은 세션에서 쓸 수 있다** — 런타임이 레지스트리를 갱신한다.
  다만 갱신은 런타임 동작이라 보장 대상이 아니다. 못 찾으면 새 세션에서 재시도한다.
- 워커를 늘리거나 없애면 **이 표 · `.claude/agents/` · `worker_path_guard.py`의 `BOUNDARIES`를 함께** 갱신한다.

### 중첩 위임은 쓰지 않는다

하네스는 **기본 3계층까지 허용**한다. 그런데도 안 쓰는 것은 **우리가 `tools:`에 `Agent`를
열거하지 않기 때문**이다 — 하네스 제약이 아니라 **정책**이다.

🔴 이 차이가 실무를 가른다 — **하네스 사실은 저절로 유지되지만
정책은 `tools:`를 계속 명시해야 유지된다.** 워커를 신설할 때 `tools:`를 빠뜨리면
**전 도구가 상속돼 중첩이 열린다.**

**에러 문구를 근거로 삼을 때는 그 문구가 참인지부터 본다** — 런타임의
`Agent is disabled for this session`은 **세션 축의 진술**이라 하네스 축으로 읽으면 오독이 된다.
